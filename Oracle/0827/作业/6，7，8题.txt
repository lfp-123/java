6，答：相同点：都不删除表定义，不同点：
truncate：截断表，不带条件，删除日志和数据，删除效率高，速度快，不能还原，属于DDL。
delete：删除数据，记录日志，删除速度慢，可以带条件，删除后可以还原。属于DML

7，使用绑定，可以减少sql的解析，从而减少了数据库引擎在sql解析上资源的消耗。提高了执行效率和可靠性。减少对数据库的访问实际上就是减少了数据库的工作量。

8，sum 统计列表的数值
    avg 统计平均值
Max列中最大值
Min 列中最小值
count 行值


